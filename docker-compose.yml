services:
  codelens:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./datasets:/app/datasets
      - chroma_data:/app/data
    environment:
      - PYTHONPATH=/app

volumes:
  chroma_data: